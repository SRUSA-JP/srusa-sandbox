/**
 * 年表を横軸で見せるときの寸法と動き。
 *
 * 見た目の実値はここだけが持ち、部品は参照するだけにする。
 */
export const HISTORY_AXIS = {
  /** 人数を表すブロック 1 個の一辺（px）。マイクラの積み木に合わせて四角にする。 */
  blockSize: 14,
  /** ブロックとブロックのあいだ（px）。 */
  blockGap: 2,
  /** 1 段に積むブロックの数。これを超えたら上へ積み上がる。 */
  blocksPerRow: 20,
  /** 出来事の印の直径（px）。 */
  markSize: 12,
  /** 軸の太さ（px）。 */
  trackHeight: 6,
  /** 自動で進めるときの 1 コマの間隔（ミリ秒）。 */
  stepMs: 260,
} as const;
