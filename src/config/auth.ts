/**
 * 入口の合言葉。
 *
 * 画面側だけの簡単な仕切りで、ブラウザに配られる中身に必ず入る。
 * 秘密にできるものではないので、身内に URL を配るときの目隠しとして扱う。
 * 本当に見せたくないものは、ここではなく配信側で止める。
 *
 * 検査（scripts/check-viewport.ts）も同じ値を使うので、他を読み込まない
 * 小さなファイルに置く。画面の部品から取ると、アプリ全体を引き込んでしまう。
 */
export const AUTH_PASSWORD = 'Srusa1234！';
