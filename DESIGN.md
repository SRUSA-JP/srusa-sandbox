# DESIGN.md — srusa-sandbox のデザインの決まり

この画面をどう見せるかの決まりをまとめたもの。
**実装の決まり（層の分け方、コミット規則など）は [CLAUDE.md](CLAUDE.md)、使い方は [README.md](README.md)。**

値そのものはコードが持つ。この文書は「なぜその値なのか」と「どう使い分けるか」を書く。

| 決めていること | 実際の値がある場所 |
| --- | --- |
| 色 | [src/theme/palette.ts](src/theme/palette.ts) |
| アイコン・タイトル・説明（タブとホーム画面） | [src/config/pwa.ts](src/config/pwa.ts) |
| プレイヤー仮アイコンの色 | [src/config/playerIcons.ts](src/config/playerIcons.ts) |
| 文字の大きさ・余白・角丸・線・寸法 | [src/theme/tokens.ts](src/theme/tokens.ts) |
| 部品ごとの色の割り当て | [src/config/colors.ts](src/config/colors.ts) |
| ページごとの見た目（スキン） | [src/config/skins.ts](src/config/skins.ts) |
| グラフの寸法 | [src/config/charts.ts](src/config/charts.ts) |
| ホームの更新ログとジャンプ先 | [src/content/home.ts](src/content/home.ts) |

UI は Claude / Codex のどちらで編集する場合も、[CLAUDE.md](CLAUDE.md) のアトミックデザイン方針に従う。
見た目の判断はこの文書、部品の層分けは `atoms` → `molecules` → `organisms` → `templates` の一方向で揃える。
page は画面の組み立てだけを持ち、再利用できる表示や操作は適切な component 層へ逃がす。

---

## 1. 何を大事にするか

1. **主役を先に見せる。** ページの上から順に、数字 → 図 → 操作 → 説明 → 注意書き。
   読まなくても困らないものほど下に置く
2. **読めることを最優先する。** 文字と背景のコントラストは常に WCAG AA を満たす。
   満たせない配色は採用しない（`npm run check:contrast` が落ちる）
3. **同じものは同じに見せる。** 同じ役割の部品は同じ余白・同じ角丸・同じ文字の大きさにする。
   そのために値は必ずトークン経由で使う（`npm run check:design` が直書きを見る）
4. **飾らない。** 影・グラデーション・アニメーションは使わない。区切りは余白と細い線だけで作る。
   データそのものが主役で、装飾はデータの読み取りを邪魔する

---

## 2. タイポグラフィ

### 大きさ

`FONT_SIZE`（[src/theme/tokens.ts](src/theme/tokens.ts)）が唯一の定義場所。
隣り合う段が 1〜2px ずつ上がる、ゆるやかなモジュラースケール。

| トークン | px | 使うところ |
| --- | --- | --- |
| `xxs` | 10 | 図中の添え字（散布図の点の名前）。**読ませる文には使わない** |
| `xs` | 11 | 補足・データラベル・相関図のノード名 |
| `sm` | 12 | 軸・凡例・注記 |
| `md` | 13 | 表・操作部品・小見出し |
| `base` | 15 | 本文 |
| `lg` | 17 | 節の見出し |
| `xl` | 20 | 画面の見出し |
| `display` | 24 | 指標タイルの数字 |

**なぜ本文が 15px か。** 一般的な読み物では 16px 以上が薦められるが、この画面は
表とグラフが主役で、1 画面に入る情報量を優先している（GitHub や Linear のような
データ密度の高い画面と同じ考え方）。小さくした分は行の高さと余白で補う。

**なぜ段の差が小さいか。** この画面は「同じくらいの重みの文字」をたくさん並べる。
段を大きく開けると見出しだけが浮いて、一覧性が落ちる。

### 太さ・行・字間

- 太さは `normal`(400) / `medium`(600) / `bold`(650) の 3 段だけ。
  強調は太さで行い、色を変えて強調しない（色は意味に使う）
- 行の高さは本文 `base`(1.6)、詰めたいときだけ `tight`(1.3)
- 字間を詰める（`tracking-tight`）のは画面の見出しと大きな数字だけ

### 書体

本文と UI は端末の標準書体（`FONT_FAMILY.base`）。数字を並べて比べる表は
等幅（`FONT_FAMILY.numeric`）と `tabular-nums` で桁を揃える。

---

## 3. 余白

`SPACE`（[src/theme/tokens.ts](src/theme/tokens.ts)）が唯一の定義場所。
**4px を 1 目盛りとするグリッド。** 小さい側だけ 2px の半段（`sm` = 10）を許す
（Tailwind の既定の刻みと同じ考え方）。目盛りから外れた値は足さない。

| トークン | px | 使うところ |
| --- | --- | --- |
| `xxs` | 4 | 文字と文字（ラベルとその値） |
| `xs` | 8 | ごく近いもの同士（箇条書きの行、凡例の項目） |
| `sm` | 10 | 操作部品の内側・上下 |
| `md` | 12 | 入力欄の内側・左右、小さな並び |
| `lg` | 16 | 操作部品の内側・左右、部品どうし |
| `xl` | 24 | ひとまとまりの中のブロック間 |
| `xxl` | 32 | まとまりとまとまりの間 |
| `xxxl` | 40 | 画面の左右の余白（広い画面） |
| `section` | 64 | 節と節の間 |
| `page` | 96 | ページ下端 |

**近接の原則**で意味のまとまりを作る。関係が近いものほど近くに置き、
節の切れ目は線ではなく大きな余白（`section`）で示す。
1 段違いが見分けられないと余白が意味を持たなくなるので、段の差ははっきり付ける。

### 操作部品の高さ

ボタン・プルダウン・入力欄の高さは、**[src/components/classes.ts](src/components/classes.ts) の
`CONTROL_BOX` だけが決める。** 高さは次の式で決まる。

```text
高さ = 上下の余白（xs = 8）× 2 + 文字の行の高さ（md 13px × tight 1.3）+ 枠線 × 2
     ≒ 35px（ドット絵風のスキンでは 38px）
```

操作部品だけ行の高さを `tight` にしているのは、本文の `base`(1.6) をそのまま使うと
中身に対して縦に伸びて見えるため。余白ではなく行の高さが効いているので、
「余白を詰めても縦長のまま」という状態になりやすい。まず行の高さを疑うこと。

触れる大きさは WCAG 2.2 の下限（24 × 24px）を満たす。それより大きい
指の当たりやすさ（44px 前後）は、部品どうしの間隔（`md` = 12px）で補う。
高さを変えるときは `CONTROL_BOX` の 1 行だけを変える。個々の部品で上書きしない。

---

## 4. 色

### 考え方

色は 3 段階を通って画面に出る。**部品は色を直接指さず、必ず役割を指す。**

```
theme/palette.ts   色そのもの（VizTheme）
      ↓
config/colors.ts   役割（ColorRole）… 「面」「文字」「強調」など
      ↓
config/colors.ts   部品（PART_COLORS）… 「表の見出しの背景」など
      ↓
styles/index.css   Tailwind のトークン（bg-table-head など）
```

配色を変えるなら palette.ts、ある部品だけ色を変えたいなら PART_COLORS の 1 行を差し替える。
UI層に色コード、`rgb()`、`text-white` / `bg-black` のような標準色クラスを直接置くと
`npm run check:design` が落ちる。例外を増やすより、まず `config/colors.ts` の役割色へ足す。

### 面（奥行き）

影を使わないので、**奥行きは明度だけで表す。**
明るい配色でも暗い配色でも「手前ほど明るい」で統一する。

| 役割 | 使うところ |
| --- | --- |
| `background` | ページの地。いちばん奥 |
| `surface` | カード・グラフの面。**文字のコントラストはこの面を基準に判定する** |
| `raised` | ボタン・入力欄。面から一段持ち上げる |
| `sunken` | 表の見出し行・注意書き。面から一段沈める（沈む側は常に暗い） |
| `hover` | 指したときに敷く面 |
| `overlay` | ツールチップ。面の上に浮かせる |

### 文字（3 段）

| 役割 | 使うところ |
| --- | --- |
| `text` | 本文・見出し |
| `muted` | 補足・ラベル・軸 |
| `subtle` | 件数・単位など、拾い読みされる添え物 |

**3 段とも AA（4.5:1）を満たす。** 段の違いは強さの表現であって、
コントラスト基準を下げる口実にしない。
これはコードで保証している。`roleColors()` が、文字が載りうる面のうち
いちばん条件の厳しい面（＝ `hover`）に対して基準を満たすところまで色を寄せてから返す。
スキンが地の色を差し替えても、手で色を選び直す必要はない。

### 強調と状態

- `accent`: 選択状態・強調。`accentHover` は指したとき、`accentSubtle` は薄く敷く面
- 配色の切り替えは「いま見えている配色の逆」にする。端末の設定に従っている間も、
  押した瞬間の見た目から反転するので、押し心地が予想どおりになる
- `danger`: **いま困っていること**だけに使う。常時出ている注意書きに赤を使うと、
  本当のエラーが埋もれる（注意書きは `sunken` の面で静かに出す）
- 状態は色だけで伝えない。選択中のタブは「下線 + 面 + 太さ」で示す

### 系列色（グラフ）

`categorical` の 8 色。隣接ペアの色覚差（ΔE ≥ 8）と明度帯・彩度下限を検証済み。
**8 色を超える分は「その他」に畳む**（`LIMITS.breakdownItems`）。9 色目を足さない。

系列色は**登録順（スロット順）で固定**する。ランキング順で色を付け替えると、
絞り込んだだけで色が入れ替わって別のものに見えてしまう。

### グラフに数字を書く場所

- **積み上げ棒グラフの中には数字を書かない。** 細い帯に数字が重なって読めず、
  色の面の上に載るので文字色も安定しない。積み上げで読み取らせるのは
  「全体の大きさ」と「割合」までにして、正確な値はツールチップに任せる
  （代わりにツールチップへ合計を添える）
- 横棒・横並び棒・折れ線は、図形の外に余白があるのでそのまま数字を出す
- 散布図は点の上に値、下に名前を出す。重なるときは値を落とす

---

## 5. 形

- **角丸** `RADIUS`: 凡例のドット `xs` / 小さな四角 `sm` / 操作部品・カード `md` / 囲み `lg` / 丸い札 `pill`
- **線** `BORDER`: 通常は `hairline`(1px)、選択中の下線とフォーカスの輪郭は `thick`(2px)。
  スキンで倍率が変わるので、Tailwind の `border-2` ではなく `border-hairline` / `border-thick` を使う
- **アイコン**: 24 の座標系に太さ 2 の線だけで描き、表示は 16px。塗りは使わない。
  絵そのものは読み上げから外し、意味は隣の文字が持つ。
  文字を置けない場所（配色の切り替え）だけ絵だけのボタンにし、
  読み上げ用の説明を必ず付ける。説明は状態ではなく**押すと何になるか**で書く
- **Minecraft のプレイヤー仮アイコン**: プレイヤー名から固定生成する 8 × 8 のピクセル顔として扱う。
  色は [src/config/playerIcons.ts](src/config/playerIcons.ts) に置き、部品は
  [src/components/molecules/PlayerIconPlaceholder.tsx](src/components/molecules/PlayerIconPlaceholder.tsx) に閉じ込める。
  人物カードや切替スロットのような意味のあるまとまりは organisms 側で組み立てる。
- **Discovery / Inventory 風スロット**: 記録や実績は、横長カードだけでなく正方形に近いスロットとして並べてよい。
  広い画面では列数を増やし、狭い画面では `auto-fit` と `minmax()` で自然に折り返す。
  スロット内のアイテム風アイコンは装飾ではなく、採掘・移動・死亡・戦闘などの意味と対応させる。
- **Minecraft 風テクスチャ**: 公式テクスチャ画像は同梱せず、ピクセル状の面・グリッド・凹凸を CSS で作る。
  石やインベントリ枠の質感は [src/lib/minecraftTextures.ts](src/lib/minecraftTextures.ts) に閉じ込め、
  コンポーネント側に色や影の実値を散らさない。
- **Minecraft 統計ページの密度**: トップはゲーム画面の HUD / Inventory として見せるため、
  Hero・Overview・Discovery・Filter の間は `section` ではなく `xxl` 程度に詰める。
  説明文は最初に大きく挟まず、下部の読み物に回して、初期表示で主役の統計画面がすぐ見えるようにする。
- **維持費の試算**: お金の話は目立たせすぎず、統計の 1 パネルとして扱う。
  入力は `NumberField`、要約は `KpiTile`、配分は既存の横棒グラフと表で見せる。
  金額の内訳は CSV で持ち出せるようにし、判断根拠（基本割・傾斜係数・対象者）は注記か表に残す。
- **ホームの更新カード**: 更新ログは、更新 1 件をカード 1 枚として扱う。
  使う面・線・札は既存の `bg-surface`、`border-hairline`、`TAG` に揃え、ページ全体や節全体を
  さらにカードで囲まない。写真は更新内容そのものを示す既存画像だけを使い、雰囲気だけの飾り画像は置かない。
  カード本文とジャンプ先は [src/content/home.ts](src/content/home.ts) に集め、表示部品へ文言を直書きしない。
- **長い設定欄**: 表示設定・詳細設定・デバッグ用の調整値のように、画面上部を長く占有するものは
  `<details>` か `TechnicalDetails` にたたむ。上部に常時置く操作は、その場で頻繁に触るものだけに絞る。
  折りたたみの summary は既存の操作部品と同じ面・線・文字サイズにし、開いた中身も `gap-*` と
  `border-hairline` で区切る。長いラベルや選択肢は折り返し・横スクロールできる場所へ逃がし、
  見出し行に詰め込まない。

---

## 6. レイアウト

### ページの並び

上から順に、**主役 → 操作 → 説明 → 注意書き → 作り手向けの詳細 → 出典**。

```
見出し ＋ ひと目で分かる規模・日付        ← 詳しい出典は下へ
いま困っていることの知らせ（エラーだけ）
この画面の要約（1 段落）
────────────────────────
画像・地図・図（見て分かるもの）
指標タイル（数字そのものが主役）
絞り込み
グラフ
────────────────────────
解説（使い方・読み方・データの注意）
注意書き（試験的である、など）
作り手向けの詳細（たたんである）        ← 生成ログ・出典・検証・JSON の読み書き・構成
出典・バージョン・単位
```

`AppLayout`（[src/components/templates/](src/components/templates/)）がこの順を持つ。
ページはスロットに中身を入れるだけで、順番を決めない。

### ヘッダー

ヘッダーはサイト名、ロゴ、配色切替、情報カテゴリの切替だけを持つ。
説明文はヘッダーに置かず、各ページの lead やホームの更新ログへ回す。
ナビはヘッダー直下に近づけ、上部全体をコンパクトな切替領域として扱う。
ロゴはホームへのリンクにし、サイズは `logoSize` トークンで揃える。
PC 幅ではヘッダーをウィンドウ上端に固定し、1 段の横並びにする。入りきらない場合も折り返さず、
ヘッダー内の横スクロールで吸収する。
親ジャンルは `ホーム / ゲーム / 人 / 記録 / ギャラリー` の 5 つを基本にする。
子の切替は親ジャンルの直下に置き、ゲームだけは `ゲーム名 → そのゲーム内の画面` の順に 2 段まで許す。
人は `メンバー / 相関図`、記録は `年表 / イベント` のように、スマートフォン下部ナビと同じ包含関係で並べる。

スマートフォン幅では、親カテゴリの切替を画面下端の固定タブに移す。PayPay や LINE の下部ナビのように
「アイコン + 短いラベル」で 5 項目前後にまとめ、近い概念はまとめてよい（例: 相関図とメンバーは「人」、
年表とイベントは「記録」）。選択状態は面・太さ・色で示し、本文は `mobileNavPagePadding` で下部ナビに
隠れない余白を取る。ヘッダー側はロゴと配色切替だけを残し、2 段化させない。
スマートフォンの下部ナビは高さを抑え、アイコンを主役にする。画面上の文字を省く場合も、読み上げ用の
ラベルは残す。
選択中の項目は角丸の四角で囲まず、フッター上端と揃う細いインジケータとアイコン色で示す。
下部ナビのアイコンはドット絵 atom を使い、線アイコンと混ぜない。親ジャンル内の切替は下部ナビの直上に
固定し、現在の親ジャンルに関係する画面だけを出す。

### 作り手向けのものは上に置かない

**画面の上半分は、見に来た人の目当てだけに使う。** 画像・地図・グラフのように
見た瞬間に分かるものを先に出し、次の 3 つは `AppLayout` の `technical` スロットへ回して、
[`TechnicalDetails`](src/components/organisms/TechnicalDetails.tsx) でたたんでおく。

| 例 | どこへ |
| --- | --- |
| PNG の画素数・ファイルの大きさ・生成ログ | `technical`（たたむ） |
| 取得元のパス・インスタンス ID・データの検証結果 | `technical`（たたむ） |
| JSON の読み込み・書き出し、配置の持ち出し | `technical`（たたむ） |
| ファイル数や容量の見積り、作り直す手順、TODO | 読み物の節に `audience: 'builder'` を付ける |

### JSON で持ち運ぶ入力ツール

DB を持たない入力ツール（ポイント集計など）は、ブラウザのローカル保存と JSON の読み込み・書き出しを
同じ画面の主要操作に置く。入力中の作業は自動保存し、別端末・後日・途中再開には JSON を使う。
CSV は確認用の表出力、JSON は状態復元用として役割を分ける。ゲーム名・プレイヤー名のようなユーザーが
場に合わせて変える名前は固定候補に閉じず、画面上で直接編集できるようにする。
スコア記録は、入力する人が紙の得点表のように上から埋められることを優先する。表は「行＝回・カテゴリ」
「列＝参加者」を基本にし、行見出し・列見出し・合計行・行合計を同じ枠内に置く。入力状況や現在の先頭は
表の直上に短く置き、順位表やCSV用の一覧は記録表の後ろに回す。
列が多い場合は表の枠だけを横スクロールさせ、横に続くことが分かるよう外枠と切れた列を見せる。
HTML は `caption`、`scope="col"`、`scope="row"` を使い、見た目だけで行列関係を伝えない。

### ホーム

ホームはサイト全体の入口で、ランディングページではなく更新ログ兼ジャンプページとして扱う。
構成は `AppLayout` の見出し、短い lead、`更新ログ`、`ジャンプ` の順にする。

- `更新ログ` は最新・重要な更新を上から並べる。主要更新は画像付きでもよいが、画像は地図・3Dビュー・スクリーンショットなど更新内容を直接示すものに限る
- 更新カードは一覧の 1 項目なのでカード化してよい。節全体をさらに枠で囲まず、`SectionHeader` と余白で区切る
- 更新カードには日付、カテゴリ、見出し、要約、要点を入れる。生成ログや検証コマンドの詳細は `TODO.md` や各ページの `technical` に置き、ホームには出さない
- `ジャンプ` は主要ページへの入口だけに絞る。説明は 1 行程度にし、操作説明や機能一覧を長く書かない
- `ジャンプ` は下部ナビと同じ親ジャンルでインデントし、子ページへ直接飛べるようにする
- ホームの文言・リンク・画像パスは [src/content/home.ts](src/content/home.ts) に集める。見た目は [src/components/organisms/HomeUpdateBoard.tsx](src/components/organisms/HomeUpdateBoard.tsx) が持つ

### ワールドマップ

ワールドマップは `スポーン周辺 3D`、`2D マップ` の順に常時表示する。
3D は実際に動かせるビューアがある場合はスクリーンショットで代替しない。
2D のディメンション、履歴、ツールチップ切替は補助操作なので、カード右上の `2D マップ設定` にたたむ。
生成ログ、作り直し手順、公開範囲の判断材料は `地図の詳細` にたたみ、通常の閲覧導線へ出さない。
3D ビューアは PC でもスマートフォンでもページ内の主役なので、通常表示から画面高の大半を使う。
スマートフォンでは、縦長画面で見下ろすだけにならないように `worldMap3dCompactHeight` と
`worldMap3dMinHeight` を広めに取る。全画面時は `dvh` 系の高さでブラウザUIの出入りに追従させる。
物理的な画面高さが足りない場合は、iframe の内側を `worldMap3dScale` で縮小して論理ビューポートを広げる。
スマートフォンではこの広い表示を既定にし、PC では `広く見る` ボタンで切り替えられるようにする。
BlueMap 生成物本体は Git 追跡しないため、スマートフォン向けに BlueMap 内部 UI を補正する CSS は
[src/components/organisms/WorldMap3dViewer.tsx](src/components/organisms/WorldMap3dViewer.tsx) の iframe 注入に閉じ込める。
補正対象は `#app`、`.control-bar`、`#ff-mobile-controls` など、BlueMap 側の固定UIだけにする。

読み物は [src/content/](src/content/) の節ごとに `audience` を持ち、
`readerSections()` が本文へ、`builderSections()` が `technical` へ振り分ける
（[src/content/audience.ts](src/content/audience.ts)）。ページは節の中身を知らない。

赤い枠（`Note tone="error"`）は「いま困っていること」だけに使う。
データの不整合のような、見に来た人には手の打ちようがないものは上の知らせに出さず、
`technical` の中に置く。

### 幅

- 本文全体は `LAYOUT.maxWidth`(1180px) で中央寄せ
- **読み物は `proseMaxWidth`(760px) で折り返す。** 1 行が長すぎると視線が戻る場所を見失う

### ウィンドウからはみ出さない

**どのページも、ウィンドウの幅を超えない。** ページ全体に横スクロールを出さない。
中身が入りきらないときは、その中身を入れた**枠の内側**で吸収する。

| 入りきらないもの | どうするか |
| --- | --- |
| 大きな図（ワールドマップ・相関図） | 高さを決めた表示枠に入れ、枠の中で掴んで動かす・拡大縮小する（`ViewportFrame`） |
| 列の多い表 | その表の枠だけ横スクロールさせる |
| 長い ID・URL | 折り返す |

図を「実寸のまま置いて画面ごと横スクロールさせる」ことはしない。
図の外の見出しや説明まで一緒に流れてしまい、どこを見ているか分からなくなるため。

表示枠の高さは `LAYOUT.viewportHeight`(640px) / 狭い画面では `viewportCompactHeight`(420px)。
全体が入る必要は無い（枠の中で動かせる）が、見出しと下の説明が同時に見える高さで止める。

#### 図の中の文字（SVG）

図の中の文字も枠（`viewBox`）からはみ出さない。**はみ出した文字は黙って切り取られる**ので、
画面を見ないと気づけない。次の 3 つを守る。

1. **位置は計算で決める。** 軸の名前のように外側へ伸びるものは、字数から幅を見積もって
   （[src/lib/text.ts](src/lib/text.ts)）、枠に収まる半径を先に求める
   （[src/lib/radar.ts](src/lib/radar.ts)）。半径を決め打ちにすると、文言を変えた日に切れる
2. **文字の大きさは `FONT_SIZE` から取り、`skinnedFontSize()` を通す。** `fontSize="5.4"` のような
   直書きはスキンの倍率が効かず、読めない大きさになる（`check:design` が落とす）
3. **図を引き伸ばさない。** `viewBox` の座標で描いた文字は、枠を広げるとそのまま拡大される。
   表示は設計寸法（例: `LAYOUT.playstyleRadarSize`）で止め、置き場所の列の最小幅もその値に合わせる

軸の名前が長いと、その分だけ図そのものが小さくなる。名前は短くし、
正式な名前は図の隣の一覧で見せる。潰れていないか（`RADAR.minDiameterRatio`）は
`npm run check:layout` が見る。

| 検査 | 何を見るか |
| --- | --- |
| `npm run check:layout` | スキンごとの文字の大きさ、軸の名前が枠に収まるか、図が潰れていないか |
| `npm run test:ui` | 描き上がった各ページの SVG に、はみ出した文字が無いか |
| `npm run check:design` | 文字の大きさを数値で直書きしていないか |

#### ワールドマップ

ワールドマップは「実際の地形を読む図」として扱う。公開画面に出す地図は
BlueMap の通常地形色（草地の緑、海の青、雪の白など）が載った PNG だけにする。
チャンクの有無やコンテナ位置だけを色で塗った簡略マップは、調査用の中間成果物としては使ってよいが、
同じ画面に混ぜない。見た目が違う地図が同列に並ぶと、利用者は縮尺差ではなく別種の情報として読んでしまう。

範囲が広くても方針は変えない。大きい地図は `ViewportFrame` の中で掴んで動かす・拡大することで吸収し、
色や表現を簡略化しない。日付の切り替えは 1 つのプルダウンだけで行い、最新日は
`最新（YYYY-MM-DD）`、過去分は `YYYY-MM-DD` と表示する。

#### 実装するときの注意

**`flex` / `grid` の子には `min-w-0` を付ける。** 既定の `min-width: auto` は
「中身の最小幅」を意味するので、実寸 1,900px の図を入れた枠は 1,900px まで広がり、
`overflow: hidden` を付けていてもウィンドウからはみ出す。

確かめ方（ウィンドウを狭くして、横スクロールバーが出ないこと）:

```js
document.documentElement.scrollWidth <= document.documentElement.clientWidth
```

### レスポンシブ

境目は `BREAKPOINT`（`compact` = 640px、`narrow` = 768px）。
Tailwind の `sm:` `md:` と同じ値に揃えてある
（CSS のメディアクエリはカスタムプロパティを読めないので、ここだけは値が 2 か所にある）。

| 何を | 狭い画面 | 広い画面 |
| --- | --- | --- |
| 画面の左右の余白 | `lg`(16) | `xxl`(32) → `xxxl`(40) |
| 配色の切り替え | ボタン 1 つ（どの幅でも同じ） | ボタン 1 つ |
| グラフの高さ・軸のラベル列・枠の余白 | `COMPACT`（[src/config/charts.ts](src/config/charts.ts)）に差し替え | 既定値 |
| 大きな図（ワールドマップ・相関図） | 枠を低くする（`viewportCompactHeight`） | 枠の高さは `viewportHeight` |
| 表・長い ID | 枠の中で横スクロール / 折り返し | そのまま |

グラフの寸法を切り替える判断は `useChartMetrics()` の 1 か所だけにある。
コンポーネントは「今の画面に合った値」を受け取るだけで、幅を見ない。

### 相関図

相関図は、人物・所属・関係を同時に読むための地図として扱う。
主役は「誰がどこに属し、誰とつながっているか」で、装飾よりも重なりの少なさを優先する。

- **人物ノード**: 1 人につき 1 ノードだけを描く。同じ人物を所属ごとに複製しない。
  複数の所属は、ノードの複製ではなくグループ領域の重なりで表す
- **グループ領域**: 所属者を囲う面として描き、面積が大きい領域を奥、小さい領域を手前に置く。
  大学・研究室・学校などの入れ子は、領域の重なりとラベルで読む
- **関係線**: 人物の位置を読ませる補助線。常に主張しすぎない色と太さにし、
  強調時だけ線・関連ノード・領域のコントラストを上げる
- **中心人物と強調**: 中心人物や選択中グループは、色だけでなく太さ・面・表示順でも区別する。
  状態を色だけに頼らない
- **操作**: 図は `ViewportFrame` に入れ、枠の中で掴んで動かす・拡大縮小する。
  人物ノードだけは個別に動かせるが、動かした後の領域と関係線は現在位置から再計算する
- **配置アルゴリズムの比較**: 相関図は用途によって良い配置が変わるので、複数の配置を切り替えて比較できるようにする。
  既定候補は、所属の重なりを読む `所属クラスタ`、所属を保ちつつ関係線を短くする `所属ハイブリッド`、
  関係のまとまりを読む `関係コミュニティ`、グラフ距離を平面距離に近づける `距離ストレス`、関係の近さを読む
  `力学`、中心人物からの距離を見る `放射状`、分類の段を読む `階層`、主所属ごとの島を読む `所属リング`、
  ハブと周辺を読む `コア周辺`、全員を同条件で見比べる `円形`。
  Graphviz の `neato/fdp`（力学）、`dot`（階層）、`twopi`（放射状）、`circo`（円形）に相当する考え方、
  `osage` / `patchwork`（クラスタ配置）、force-directed placement、ラベル伝播系コミュニティ、
  MDS / stress 系レイアウトを参考にする。
  手動で動かした座標はアルゴリズムごとに意味が変わるので、配置を切り替えたらリセットする
- **責務の分け方**: 配置は [src/map/layout.ts](src/map/layout.ts)、見た目の判断は
  [src/map/display.ts](src/map/display.ts)、描画部品は
  `GroupRegion` / `PersonNode` / `RelationEdge` / `RelationshipMap` に分ける。
  コンポーネント内で分類色・線幅・ラベル文言を直接決めない

---

## 7. スキン

書体・角丸・線の太さ・テーマ色をまとめて差し替えるプリセット
（[src/config/skins.ts](src/config/skins.ts)）。どの画面がどれを使うかは
[src/routes.ts](src/routes.ts) が決める。

| スキン | 見た目 |
| --- | --- |
| `default` | 標準 |
| `minecraft` | ドット書体・角丸なし・線 2 倍・草の緑。図形の輪郭もぼかさない |

スキンは**トークンの差し替えだけ**で成立させる。スキンごとの分岐をコンポーネントに書かない。

---

## 8. 変えるときの手順

1. 値を変える（上の表のファイル）
2. `npm run check:contrast` を通す（スキン × ライト / ダークの全 4 通りを検査する）
3. ライト / ダーク × 標準 / ドット絵風の 4 通りと、狭い画面・広い画面で見え方を確かめる
4. コンポーネントに実値を直書きしていないか確かめる

   ```shell
   grep -rnoE '\b[a-z-]+-\[[^]]*\]' --include='*.tsx' src/ | grep -v 'var(--sr-'
   ```

### やらないこと

- ページ全体に横スクロールを出す（はみ出しは枠の内側で吸収する）
- 余白トークンに `none`(0) の段を足す。Tailwind は `--spacing-*` の名前を
  `max-w-*` `rounded-*` にも配るので、`max-w-none` が `max-width: 0` に化けて要素が消える
- 影・グラデーション・角度のついた装飾を足す
- 目立たせたいボタンを増やす（全部目立つと、どれも目立たない）
- 色だけで状態を伝える
- トークンにない px・色コードを書く
- 検証していない系列色を足す
